# KaamMitra Environment Configuration

# Database Connection
# Required - PostgreSQL connection string for your local or cloud database
DATABASE_URL=postgresql://user:password@host:port/database

# Email Service Credentials (for verification emails)
# Required - Email account credentials
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password

# Authentication
# Required - Random string for session encryption
SESSION_SECRET=your_random_secret_string

# Server Configuration
# Optional - Defaults to 3000
PORT=3000

# File Upload Configuration
# Optional - Defaults to ./uploads
UPLOAD_DIR=./uploads

# Optional - Email verification configuration
# Number of minutes until verification code expires
VERIFICATION_CODE_EXPIRY_MINUTES=30

# Optional - Database connection pool configuration
DB_POOL_MIN=1
DB_POOL_MAX=10

# Optional - WhatsApp API configuration (if enabled)
# WHATSAPP_TOKEN=your_whatsapp_api_token
# WHATSAPP_PHONE_NUMBER_ID=your_whatsapp_phone_number_id

# Optional - Geolocation API key (if enabled)
# GEOCODING_API_KEY=your_geocoding_api_key